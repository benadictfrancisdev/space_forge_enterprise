# SpaceForge Enterprise Frontend
# Copy to `.env` and fill from your enterprise environment.

# Django REST base (Track 2+)
VITE_API_BASE_URL=http://localhost:8000

# Auth bridge: dev | firebase
# Use "dev" when backend AUTH_MODE=dev (local lite)
VITE_AUTH_BRIDGE=dev
